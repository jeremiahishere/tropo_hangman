<%= form_for(@game) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :word_id %><br />
    <%= f.text_field :word_id %>
  </div>
  <div class="field">
    <%= f.label :in_progress %><br />
    <%= f.check_box :in_progress %>
  </div>
  <div class="field">
    <%= f.label :guessed_letters %><br />
    <%= f.text_field :guessed_letters %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
